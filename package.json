{
  "name": "bballdaniel3.com",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "docker-compose build",
    "start": "docker-compose up -d",
    "restart": "docker-compose down && docker-compose up --build -d",
    "nodemon": "nodemon -e \"*\" app/server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.2",
    "express-handlebars": "^8.0.3",
    "fs": "^0.0.1-security",
    "gray-matter": "^4.0.3",
    "marked": "^15.0.11",
    "swup": "^4.8.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
